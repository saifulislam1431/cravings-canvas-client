import React, { useContext } from 'react';
import { UserContext } from '../../AuthProviders/AuthProvider';

const Home = () => {
   
    return (
        <div>
           <h1>Home.....!</h1>
        </div>
    );
};

export default Home;